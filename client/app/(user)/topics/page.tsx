import TopicsList from "@/components/user/topics/TopicsList";

const TopicsPage = () => {
    return (
        <section className="pt-5">
            <TopicsList />
        </section>
    );
};

export default TopicsPage;
